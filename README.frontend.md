# Frontend — Professional Manager Dashboard

- Built with React + Vite + Tailwind + Framer Motion.
- Uses API base from VITE_API_BASE.
- To run:
  - npm install
  - npm run dev

Design notes:
- Left sidebar for navigation and quick search.
- Center panel: chat thread with rich bubbles and composer.
- Right panel: AI summary (date-range), suggestions (bot-assisted replies), booking panel (carousel + slots).
- Animations: Framer Motion is used for micro-interactions.
